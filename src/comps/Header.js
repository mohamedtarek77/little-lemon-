import React from 'react'
import {logo} from '../assets'
const Header = () => {
  return (
    <nav>

    <img alt='logo' src={logo} />
    <ul>
        <li>
            
        </li>
    </ul>
    </nav>
  )
}

export default Header